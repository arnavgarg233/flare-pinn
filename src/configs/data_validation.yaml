# Data configuration for validation set (15% of data, for hyperparameter tuning)
span:
  start: "2015-01-11"
  end: "2015-12-18"

cadence_minutes: 60
window_hours: 48
window_stride_hours: 6
targets_hours: [6, 12, 24]

censoring:
  cmd_deg_threshold: 70.0

resize:
  target_px: 128

paths:
  raw_dir: "data/raw"
  interim_dir: "data/interim"
  processed_dir: "data/processed"
  cache_dir: "data/cache"
  logs_dir: "logs"

